# Source-of-truth dependency list for `pip-compile` (CI update-deps workflow).
# Real version pins live in pyproject.toml; this file is mirrored so pip-tools
# can produce a fully resolved requirements.txt lockfile when needed.
httpx>=0.27,<1.0
pydantic>=2.7,<3.0
typer>=0.12,<1.0
rich>=13.7,<16.0
platformdirs>=4.2,<5.0
python-dateutil>=2.9,<3.0
